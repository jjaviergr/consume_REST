apply plugin: 'com.android.application'

android {
    compileSdkVersion 4
    buildToolsVersion "23.0.3"

    defaultConfig {
        applicationId "com.authorwjf.http_get"
        minSdkVersion 4
        targetSdkVersion 4
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
